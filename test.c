#include <stdio.h>
#include "ft_printf.h"
#include "libft/libft.h"


int		main()
{
	// char s[50] = "%sklj%c%xlkjl;l;kj%";
	// t_check *test;
	// int		i = 0;

	// test = check(s, 6);
	// while (i < 6)
	// {
	// 	printf("mode = %c : order = %d\n", test[i].mode, test[i].order);
	// 	i++;
	// }
	// char	str[50] = "%slk%als%jg%lj";
	// char	**split;

	// // printf("%s\n",str);
	// // trim_firstchar(str);
	// // printf("%s\n",str);

	// split = fix_firstchar(str);
	// int		i = 0;
	// while (split[i] != NULL)
	// {
	// 	printf("%s\n",split[i]);
	// 	i++;
	// }
	// i = 0;
	// while (split[i] != NULL)
	// {
	// 	free(split[i]);
	// 	i++;
	// }
	// free(split);

	// printf("%u\n",-2);
	unsigned int i,j,k;

	i = 0;
	j = 4294967295;
	k = 555;

	char	*s;

	//printf("%s\n%s\n%s\n",ft_unitoa(i), ft_unitoa(j), ft_unitoa(k));
	printf("%%\n");
}